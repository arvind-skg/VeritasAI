/**
 * CooL — Cryptographic Observability & On-chain Ledger.
 *
 * The default entry point. It gives you the high-level client, the independent
 * verifier, the typed error set, and the cryptographic primitives the evidence
 * model is built from. Everything advanced — the change-record model, direct
 * dstack clients, the quote/anchor/witness/policy machinery — lives behind
 * `cool-nwc/phala`.
 *
 *     import { CooL, verifyEvidence } from "cool-nwc";
 *
 * The SDK makes no network calls of its own and carries no telemetry.
 */
/* ── the client ──────────────────────────────────────────────────────── */
export { CooL } from "./client.js";
/* ── the verifier ────────────────────────────────────────────────────── */
export { verifyEvidence, formatVerdict, domainOrder, withTrustedKeys, } from "./verify.js";
/* ── errors ──────────────────────────────────────────────────────────── */
export { CooLError, ConfigurationError, DstackUnavailableError, AttestationRequiredError, AttestationError, EvidenceError, ClosedError, } from "./errors.js";
/* ── cryptographic primitives ────────────────────────────────────────── */
export { canonicalCbor, jsonProjection } from "./canonical.js";
export { sha256Bytes, randomSalt, saltedCommit } from "./hash.js";
export { mhSha256, multihashFromDigest, multihashDigest, isMultihash } from "./multihash.js";
export { generateKeypair, directoryFromKeypair, mergeDirectories } from "./keys.js";
export { hybridSign, hybridVerify, SIGNATURE_ALG } from "./sign.js";
export { leafHash, nodeHash, merkleRoot, inclusionProof, verifyInclusion, consistencyProof, verifyConsistency, } from "./merkle.js";
export { MemoryLog } from "./log-memory.js";
//# sourceMappingURL=index.js.map